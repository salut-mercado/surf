import { AppLayout } from "./app-layout";
import { AppRoutes } from "./app-routes";

export default function App() {
    return (
        <AppLayout>
            <AppRoutes />
        </AppLayout>
    );
}
